name = %example%

all: %DESTDIR%/resources/libraries/%example%
clean:
	rm %DESTDIR%/resources/libraries/%example%

%DESTDIR%/resources/libraries/%example%: example.cpp
	g++ -shared -Wall -Wconversion -O3 -fPIC example.cpp -o %DESTDIR%/resources/libraries/%example%
